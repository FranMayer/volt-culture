export default function Home() {
  return (
    <main
      id="main-content"
      style={{
        minHeight: "calc(100vh - var(--volt-ds-header-h))",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <p>VOLT Culture — migración en curso</p>
    </main>
  );
}
