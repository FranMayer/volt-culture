# Promo 2x1 línea TC — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dos buzos al precio de uno sobre los productos que el admin marque con `promo2x1`, aplicado igual en el carrito, en MercadoPago y en transferencia.

**Architecture:** Un módulo puro nuevo (`lib/promo2x1.js`) con la regla y el reparto de precios, consumido por los tres lugares que hoy duplican el cálculo del total: `lib/checkout.js` (display), `create-preference.js` (MP) y `create-transfer-order.js` (transferencia). El servidor siempre recalcula desde Firestore; lo que manda el cliente se ignora.

**Tech Stack:** Next.js App Router, TypeScript, Firebase Admin SDK, MercadoPago SDK. Tests: scripts Node sin framework (`node tests/<archivo>`).

**Spec:** `docs/superpowers/specs/2026-08-17-promo-2x1-tc-design.md`

## Global Constraints

- **El servidor es la autoridad del precio.** Nunca confiar en `price` ni en `promo2x1` que venga del body; releer siempre el doc de Firestore. Los dos endpoints ya lo hacen (`create-preference.js:191`).
- **`sum(mpItems.unit_price × quantity) === total`** — invariante no negociable. Si falla, MercadoPago cobra distinto de lo que dice la orden.
- **El shape del item de carrito en `localStorage` debe sobrevivir** (CLAUDE.md). `promo2x1` entra como campo **opcional**: un carrito viejo sin el campo se lee igual y queda falsy.
- **Módulos compartidos en `.js`, no `.ts`**, para que los tests los importen sin loader TS — mismo criterio que `lib/server/coupons.js` y `lib/cart/reducer.js`.
- **El cupón se rechaza si hay algún item con `promo2x1` en el carrito.** No acumulan.
- **El 10% de transferencia corre sobre el subtotal que ya tiene el 2x1 aplicado.**
- `npx next build` verde es obligatorio para cerrar cualquier tarea.
- No hardcodear secretos ni URLs de entorno.

---

### Task 1: La regla pura — `lib/promo2x1.js`

El corazón de la feature. Sin red, sin Firestore, sin React: entra un array de items, salen números. Todo lo demás lo consume.

**Files:**
- Create: `lib/promo2x1.js`
- Test: `tests/promo2x1.test.mjs`

**Interfaces:**
- Consumes: nada (es la base)
- Produces:
  - `computePromo2x1(items) -> { descuento: number, unidadesGratis: number, unidadesEnPromo: number }`
  - `tienePromo2x1(items) -> boolean`
  - `repartirDescuentoEnItems(items, descuento) -> Array<{...item, unitPrice: number, quantity: number}>`

  Donde `items` es `Array<{ id, title, price, quantity, promo2x1？ }>`.

- [ ] **Step 1: Write the failing test**

Create `tests/promo2x1.test.mjs`:

```js
/**
 * Tests de la promo 2x1. Uso: node tests/promo2x1.test.mjs
 * Regla: de cada par de unidades en promo, la más barata es gratis.
 */
import { computePromo2x1, tienePromo2x1, repartirDescuentoEnItems } from '../lib/promo2x1.js';

let failed = 0;
function check(label, cond) {
    if (!cond) { console.error(`FAIL — ${label}`); failed++; }
}

const buzo = (price, quantity = 1, promo = true) => ({
    id: `p${price}-${quantity}`, title: `Buzo ${price}`, price, quantity, promo2x1: promo,
});

// ── computePromo2x1 ────────────────────────────────────────────────────────
check('2 iguales → uno gratis',
    computePromo2x1([buzo(45000), buzo(45000)]).descuento === 45000);

check('1 solo → sin descuento',
    computePromo2x1([buzo(45000)]).descuento === 0);

// 45000, 45000, 38000 → ordenado desc, gratis el índice 1 → 45000
check('3 unidades → una gratis, el impar se paga',
    computePromo2x1([buzo(45000), buzo(45000), buzo(38000)]).descuento === 45000);

// 45000, 45000, 38000, 38000 → gratis índices 1 y 3 → 45000 + 38000
check('4 unidades precios mixtos → el más barato de cada par',
    computePromo2x1([buzo(45000), buzo(45000), buzo(38000), buzo(38000)]).descuento === 83000);

check('quantity 2 en una línea cuenta como dos unidades',
    computePromo2x1([buzo(45000, 2)]).descuento === 45000);

check('items sin promo no descuentan',
    computePromo2x1([buzo(45000, 1, false), buzo(45000, 1, false)]).descuento === 0);

check('items normales no afectan a los de promo',
    computePromo2x1([buzo(45000), buzo(45000), buzo(90000, 1, false)]).descuento === 45000);

check('carrito vacío no rompe', computePromo2x1([]).descuento === 0);

check('cuenta las unidades gratis',
    computePromo2x1([buzo(45000), buzo(45000), buzo(38000), buzo(38000)]).unidadesGratis === 2);

// ── tienePromo2x1 ──────────────────────────────────────────────────────────
check('detecta un item en promo', tienePromo2x1([buzo(45000, 1, false), buzo(38000)]) === true);
check('sin items en promo', tienePromo2x1([buzo(45000, 1, false)]) === false);
check('carrito vacío', tienePromo2x1([]) === false);

// ── repartirDescuentoEnItems: EL INVARIANTE DE MERCADOPAGO ─────────────────
// La suma de lo que se le manda a MP tiene que dar exactamente
// productsTotal − descuento, o el cliente paga distinto de lo que vio.
function sumaItems(items) {
    return items.reduce((s, i) => s + i.unitPrice * i.quantity, 0);
}
function totalCrudo(items) {
    return items.reduce((s, i) => s + i.price * i.quantity, 0);
}

const casos = [
    [buzo(45000), buzo(45000)],
    [buzo(45000), buzo(45000), buzo(38000)],
    [buzo(45000, 3)],
    [buzo(33333), buzo(33333), buzo(33333)],
    [buzo(45000), buzo(38000), buzo(90000, 1, false)],
    [buzo(45000, 2), buzo(38000, 3)],
    [buzo(999), buzo(1)],
];

for (const [idx, items] of casos.entries()) {
    const { descuento } = computePromo2x1(items);
    const repartidos = repartirDescuentoEnItems(items, descuento);
    const esperado = totalCrudo(items) - descuento;
    check(`invariante MP caso ${idx}: suma === total`, sumaItems(repartidos) === esperado);
    check(`invariante MP caso ${idx}: sin precios negativos`, repartidos.every((i) => i.unitPrice >= 0));
    check(`invariante MP caso ${idx}: sin cantidades <= 0`, repartidos.every((i) => i.quantity > 0));
    check(`invariante MP caso ${idx}: enteros`, repartidos.every((i) => Number.isInteger(i.unitPrice)));
}

check('sin descuento, los precios quedan intactos',
    repartirDescuentoEnItems([buzo(45000, 1, false)], 0)[0].unitPrice === 45000);

if (failed) {
    console.error(`\n${failed} test(s) fallaron`);
    process.exit(1);
}
console.log('promo2x1: todos los tests OK');
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node tests/promo2x1.test.mjs`
Expected: FAIL — `Cannot find module '../lib/promo2x1.js'`

- [ ] **Step 3: Write minimal implementation**

Create `lib/promo2x1.js`:

```js
/**
 * lib/promo2x1.js — regla de la promo 2x1. Puro: sin red, sin Firestore, sin
 * React. .js (no .ts) para que tests/promo2x1.test.mjs lo importe sin loader TS
 * — mismo criterio que lib/server/coupons.js.
 *
 * La regla: de cada par de unidades marcadas `promo2x1`, la más barata es
 * gratis. Las unidades se expanden por cantidad (quantity: 3 son tres
 * unidades), se ordenan de más cara a más barata y se regala una de cada dos.
 *
 * El servidor SIEMPRE recalcula esto desde el precio y el flag de Firestore.
 * Lo que manda el cliente se ignora.
 */

const enPromo = (i) => i?.promo2x1 === true;

/** ¿Hay al menos un item en promo? Decide el bloqueo de cupones. */
export function tienePromo2x1(items) {
    return (items || []).some(enPromo);
}

/**
 * @param {Array<{price:number, quantity:number, promo2x1?:boolean}>} items
 * @returns {{ descuento:number, unidadesGratis:number, unidadesEnPromo:number }}
 */
export function computePromo2x1(items) {
    const unidades = [];
    for (const item of items || []) {
        if (!enPromo(item)) continue;
        const qty = Number(item.quantity) || 0;
        const price = Number(item.price) || 0;
        for (let n = 0; n < qty; n++) unidades.push(price);
    }

    unidades.sort((a, b) => b - a);

    let descuento = 0;
    let unidadesGratis = 0;
    // De cada par la segunda (la más barata, porque está ordenado desc) es gratis.
    for (let i = 1; i < unidades.length; i += 2) {
        descuento += unidades[i];
        unidadesGratis++;
    }

    return { descuento, unidadesGratis, unidadesEnPromo: unidades.length };
}

/**
 * Reparte el descuento entre las líneas en promo y devuelve los items con
 * `unitPrice`, listos para MercadoPago.
 *
 * MP recibe una línea con unit_price y quantity, y lo que cobra es la suma. No
 * acepta unit_price negativo ni una línea de descuento, así que el 2x1 se
 * expresa bajando los precios unitarios. El reparto es proporcional al total de
 * cada línea; el residuo de redondeo se absorbe en la línea más cara, partiéndola
 * en dos si su cantidad no permite un ajuste exacto.
 *
 * Invariante: sum(unitPrice * quantity) === sum(price * quantity) − descuento.
 */
export function repartirDescuentoEnItems(items, descuento) {
    const lista = items || [];
    if (!descuento) {
        return lista.map((i) => ({ ...i, unitPrice: Number(i.price) || 0 }));
    }

    const promoLines = lista.filter(enPromo);
    const promoTotal = promoLines.reduce((s, i) => s + i.price * i.quantity, 0);
    const objetivoTotal = promoTotal - descuento;

    // Objetivo por línea, proporcional a su peso dentro del total en promo.
    const objetivos = new Map();
    for (const linea of promoLines) {
        const peso = linea.price * linea.quantity;
        objetivos.set(linea, Math.round((peso * objetivoTotal) / promoTotal));
    }

    // El redondeo por línea no suma exacto: el residuo va a la línea más cara.
    const sumaObjetivos = [...objetivos.values()].reduce((s, v) => s + v, 0);
    const residuo = objetivoTotal - sumaObjetivos;
    if (residuo !== 0 && promoLines.length) {
        const masCara = promoLines.reduce((a, b) => (b.price > a.price ? b : a));
        objetivos.set(masCara, objetivos.get(masCara) + residuo);
    }

    const salida = [];
    for (const item of lista) {
        if (!enPromo(item)) {
            salida.push({ ...item, unitPrice: Number(item.price) || 0 });
            continue;
        }

        const objetivo = objetivos.get(item);
        const qty = item.quantity;
        const base = Math.floor(objetivo / qty);
        const resto = objetivo - base * qty;

        if (resto === 0) {
            salida.push({ ...item, unitPrice: base, quantity: qty });
        } else {
            // qty−resto unidades a `base` + resto unidades a `base+1`.
            // Suma: base*(qty−resto) + (base+1)*resto = base*qty + resto = objetivo.
            salida.push({ ...item, unitPrice: base, quantity: qty - resto });
            salida.push({ ...item, unitPrice: base + 1, quantity: resto });
        }
    }

    return salida;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node tests/promo2x1.test.mjs`
Expected: PASS — `promo2x1: todos los tests OK`

- [ ] **Step 5: Commit**

```bash
git add lib/promo2x1.js tests/promo2x1.test.mjs
git commit -m "feat(promo): regla pura del 2x1 con reparto de precios para MercadoPago"
```

---

### Task 2: El flag en el modelo y en el admin

Un checkbox en el formulario de producto que escribe `promo2x1` en Firestore. Calcado del flag `limited`, que ya hace exactamente esto.

**Files:**
- Modify: `lib/types.ts` (interface `Product`, interface `CartItem`)
- Modify: `lib/admin/product-form.js:126` (zona de `limited` en `buildProductPayload`)
- Modify: `components/admin/ProductFormModal.tsx` (líneas 82, 116, 130, 281, ~395)
- Test: `tests/admin-product-form.test.mjs`

**Interfaces:**
- Consumes: nada de Task 1
- Produces: campo `promo2x1?: boolean` en `Product` y en `CartItem`; `buildProductPayload` lo persiste

- [ ] **Step 1: Write the failing test**

Añadir al final de `tests/admin-product-form.test.mjs`, antes del bloque `if (failed)`:

```js
// ── promo 2x1 ──────────────────────────────────────────────────────────────
const formPromo = {
    name: 'Buzo TC', category: 'buzos', line: 'TC', description: '',
    price: '45000', active: true, limited: false, promo2x1: true,
    variants: [], sizes: [], images: [],
};
check('buildProductPayload persiste promo2x1',
    buildProductPayload(formPromo).promo2x1 === true);
check('promo2x1 ausente → false',
    buildProductPayload({ ...formPromo, promo2x1: undefined }).promo2x1 === false);
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node tests/admin-product-form.test.mjs`
Expected: FAIL — `buildProductPayload persiste promo2x1` (devuelve `undefined`)

- [ ] **Step 3: Write minimal implementation**

En `lib/admin/product-form.js`, junto a la línea 126 (`limited: !!form.limited,`):

```js
        limited: !!form.limited,
        promo2x1: !!form.promo2x1,
```

En `lib/types.ts`, en `interface Product`, debajo de `limited?: boolean;`:

```ts
    /** Entra en la promo 2x1 (ver docs/superpowers/specs/2026-08-17-promo-2x1-tc-design.md). */
    promo2x1?: boolean;
```

En `lib/types.ts`, en `interface CartItem`, al final:

```ts
    /** Copia del flag del producto al momento de agregar. OPCIONAL: los
     *  carritos viejos en localStorage no lo tienen y deben seguir siendo
     *  válidos. El servidor es la autoridad — este campo es solo para mostrar. */
    promo2x1?: boolean;
```

En `components/admin/ProductFormModal.tsx`:

Línea ~82, junto a `const [limited, setLimited] = useState(false);`:
```tsx
    const [promo2x1, setPromo2x1] = useState(false);
```

Línea ~116, junto a `limited: product.limited === true,`:
```tsx
            promo2x1: product.promo2x1 === true,
```

Línea ~130, junto a `setLimited(product.limited === true);`:
```tsx
            setPromo2x1(product.promo2x1 === true);
```

Línea ~281, en el objeto `form`:
```tsx
        const form = { name, category, line, description, price, active, limited, promo2x1, variants, sizes, images };
```

Línea ~395, después del bloque completo del switch `productLimited`:
```tsx
                            <div className="mb-3 form-check form-switch">
                                <input
                                    className="form-check-input"
                                    type="checkbox"
                                    role="switch"
                                    id="productPromo2x1"
                                    checked={promo2x1}
                                    onChange={(e) => setPromo2x1(e.target.checked)}
                                />
                                <label className="form-check-label" htmlFor="productPromo2x1">
                                    Entra en promo 2x1
                                </label>
                            </div>
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node tests/admin-product-form.test.mjs`
Expected: PASS

Run: `npx next build`
Expected: `✓ Compiled successfully`

- [ ] **Step 5: Commit**

```bash
git add lib/types.ts lib/admin/product-form.js components/admin/ProductFormModal.tsx tests/admin-product-form.test.mjs
git commit -m "feat(promo): flag promo2x1 por producto en el panel admin"
```

---

### Task 3: El sello 2x1 en el catálogo y el flag en el carrito

Mostrar la promo donde el cliente la ve, y llevar el flag al item de carrito para el display optimista.

**Files:**
- Modify: `components/catalog/ProductCard.tsx:48` (junto al sello `limited`)
- Modify: `app/styles/volt-ds.css:1766` (junto a `.product-badge-limited`)
- Modify: `components/catalog/ProductDetail.tsx:75` (llamada a `addItem`)
- Modify: `components/catalog/QuickViewModal.tsx:107` (llamada a `addItem`)

**Interfaces:**
- Consumes: `Product.promo2x1` y `CartItem.promo2x1` de Task 2
- Produces: items de carrito con `promo2x1` para que `lib/checkout.js` (Task 4) lo lea

- [ ] **Step 1: Añadir el sello en la card**

En `components/catalog/ProductCard.tsx`, justo después del bloque de `product.limited`:

```tsx
        {product.promo2x1 === true && (
          <span className="product-badge-promo2x1" aria-label="Promoción dos por uno">
            2x1
          </span>
        )}
```

Nota: el sello de `limited` usa `aria-hidden`, pero "2x1" es información de precio que un lector de pantalla necesita — por eso lleva `aria-label` y no se oculta.

- [ ] **Step 2: Añadir el estilo**

En `app/styles/volt-ds.css`, inmediatamente después del bloque `.product-badge-limited` (termina en la línea ~1782). Los `!important` son consistentes con el archivo (CLAUDE.md: la limpieza de `!important` es post-migración, no se toca acá):

```css
.product-badge-promo2x1 {
    position: absolute !important;
    top: 10px !important;
    right: 10px !important;
    z-index: 2 !important;
    background: #000000 !important;
    color: #ffffff !important;
    border: 1px solid #c1121f !important;
    font-family: var(--font-ds-mono) !important;
    font-weight: 600 !important;
    font-size: 11px !important;
    text-transform: uppercase !important;
    letter-spacing: 0.08em !important;
    padding: 4px 8px !important;
    border-radius: 0 !important;
    line-height: 1.2 !important;
    pointer-events: none !important;
}
```

Va a la derecha (`right: 10px`) para no chocar con el sello "Edición limitada", que va a la izquierda.

- [ ] **Step 3: Pasar el flag al carrito**

En `components/catalog/ProductDetail.tsx`, dentro del objeto que recibe `addItem` (línea ~75), después de `variantSize: selectedSize,`:

```tsx
        promo2x1: product.promo2x1 === true,
```

Hacer el mismo cambio en `components/catalog/QuickViewModal.tsx` (línea ~107), en su llamada a `addItem`.

- [ ] **Step 4: Verificar el build**

Run: `npx next build`
Expected: `✓ Compiled successfully`

- [ ] **Step 5: Commit**

```bash
git add components/catalog/ProductCard.tsx components/catalog/ProductDetail.tsx components/catalog/QuickViewModal.tsx app/styles/volt-ds.css
git commit -m "feat(promo): sello 2x1 en el catálogo y flag en el item de carrito"
```

---

### Task 4: El total mostrado — `lib/checkout.js`

Lo que ve el cliente en el resumen. El servidor lo va a recalcular igual, pero si esto no coincide el cliente ve un precio y paga otro.

**Files:**
- Modify: `lib/checkout.js` (`computeCheckoutTotals`, línea ~88)
- Test: `tests/checkout-logic.test.mjs`

**Interfaces:**
- Consumes: `computePromo2x1` de Task 1; `CartItem.promo2x1` de Task 2
- Produces: `computeCheckoutTotals(...)` devuelve además `promoDiscount: number` y `promoUnidadesGratis: number`

- [ ] **Step 1: Write the failing test**

Añadir al final de `tests/checkout-logic.test.mjs`, antes del bloque `if (failed)`:

```js
// ── promo 2x1 en los totales del resumen ───────────────────────────────────
const SHIP = { cordoba: { cost: 3000 }, andreani: { cost: 0 } };
const buzoPromo = (price, quantity = 1) => ({
    id: `b${price}`, title: 'Buzo TC', price, quantity, promo2x1: true,
});

// 2 buzos de 45000, envío 0, pago MP, sin cupón.
const t1 = computeCheckoutTotals(
    [buzoPromo(45000), buzoPromo(45000)], 'andreani', SHIP, 'mp', null);
check('2x1 descuenta el más barato del par', t1.promoDiscount === 45000);
check('2x1 en MP: total = 45000', t1.total === 45000);

// Mismo carrito por transferencia: el 10% corre sobre el subtotal YA con 2x1.
const t2 = computeCheckoutTotals(
    [buzoPromo(45000), buzoPromo(45000)], 'andreani', SHIP, 'transfer', null);
check('2x1 + transferencia: promo intacta', t2.promoDiscount === 45000);
check('2x1 + transferencia: 10% sobre 45000 = 4500 → total 40500', t2.total === 40500);
check('2x1 + transferencia: discountAmount suma los dos', t2.discountAmount === 49500);

// Con envío: 2 buzos 45000 + envío 3000 → subtotal con promo 48000, −10% = 4800
const t3 = computeCheckoutTotals(
    [buzoPromo(45000), buzoPromo(45000)], 'cordoba', SHIP, 'transfer', null);
check('2x1 + envío + transferencia: total 43200', t3.total === 43200);

// Sin items en promo, el comportamiento viejo no cambia.
const t4 = computeCheckoutTotals(
    [{ id: 'x', title: 'Remera', price: 20000, quantity: 2 }], 'andreani', SHIP, 'transfer', null);
check('sin promo, transferencia sigue igual', t4.total === 36000);
check('sin promo, promoDiscount es 0', t4.promoDiscount === 0);

// Cupón + promo: el cupón se ignora (no acumulan).
const t5 = computeCheckoutTotals(
    [buzoPromo(45000), buzoPromo(45000)], 'andreani', SHIP, 'mp', { code: 'X', percent: 20 });
check('cupón no acumula con 2x1', t5.total === 45000);
```

Verificar que el import al tope del archivo incluya `computeCheckoutTotals` (ya debería estar).

- [ ] **Step 2: Run test to verify it fails**

Run: `node tests/checkout-logic.test.mjs`
Expected: FAIL — `2x1 descuenta el más barato del par` (`promoDiscount` es `undefined`)

- [ ] **Step 3: Write minimal implementation**

En `lib/checkout.js`, añadir al import del tope:

```js
import { computePromo2x1, tienePromo2x1 } from './promo2x1.js';
```

Reemplazar la función `computeCheckoutTotals` completa por:

```js
/**
 * Totales del resumen (paso 3) — port del cálculo de renderSummary()
 * (legacy pagos.js:487-543), sin el armado de HTML.
 *
 * Orden de las operaciones (ver spec 2026-08-17-promo-2x1-tc-design.md):
 *   productsTotal − 2x1 + envío = subtotal con promo → −10% transferencia
 * El cupón NO acumula con el 2x1: si hay promo en el carrito, se ignora.
 *
 * `subtotal` sigue siendo pre-descuento (productos + envío) porque así lo
 * muestra el resumen; `discountAmount` es la suma de TODOS los descuentos.
 *
 * @param {Array} items
 * @param {'cordoba'|'andreani'} shippingOption
 * @param {{cordoba:{cost:number}, andreani:{cost:number}}} shippingConfig
 * @param {'mp'|'transfer'} mode
 * @param {{code:string, percent:number}|null} coupon
 */
export function computeCheckoutTotals(items, shippingOption, shippingConfig, mode, coupon) {
    const productsTotal = items.reduce((sum, item) => sum + (item.price || 0) * (item.quantity || 1), 0);
    const shippingCost = shippingOption === 'cordoba' ? shippingConfig.cordoba.cost : shippingConfig.andreani.cost;
    const subtotal = productsTotal + shippingCost;

    const { descuento: promoDiscount, unidadesGratis: promoUnidadesGratis } = computePromo2x1(items);
    const hayPromo = tienePromo2x1(items);
    const couponAplicable = coupon && !hayPromo;

    let discountAmount = promoDiscount;

    if (couponAplicable) {
        const percent = coupon.percent;
        discountAmount += mode === 'transfer'
            ? Math.round(productsTotal * percent / 100)
            : productsTotal - items.reduce((sum, item) =>
                sum + Math.round((item.price || 0) * (100 - percent) / 100) * (item.quantity || 1), 0);
    } else if (mode === 'transfer') {
        // El 10% corre sobre el subtotal que ya tiene el 2x1 aplicado.
        discountAmount += Math.round((subtotal - promoDiscount) * TRANSFER_DISCOUNT);
    }

    const total = subtotal - discountAmount;
    return { productsTotal, shippingCost, subtotal, discountAmount, total, promoDiscount, promoUnidadesGratis };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node tests/checkout-logic.test.mjs`
Expected: PASS

Run: `node tests/promo2x1.test.mjs`
Expected: PASS (no debe romperse)

- [ ] **Step 5: Commit**

```bash
git add lib/checkout.js tests/checkout-logic.test.mjs
git commit -m "feat(promo): 2x1 en los totales del resumen de checkout"
```

---

### Task 5: Cobro por transferencia — `create-transfer-order.js`

El más simple de los dos endpoints: no arma ítems de MercadoPago, solo números.

**Files:**
- Modify: `pages/api/create-transfer-order.js:209-231`
- Modify: `lib/types.ts` (interface `Order`: `discountSource` y `discounts[]`)

**Interfaces:**
- Consumes: `computePromo2x1`, `tienePromo2x1` de Task 1
- Produces: documento de orden con `discounts: Array<{source, amount, percent?, detail?}>` y `discountSource: 'coupon' | 'transfer' | 'promo2x1' | null`

- [ ] **Step 1: Ampliar los tipos de la orden**

En `lib/types.ts`, en la `interface Order`, reemplazar la línea de `discountSource` por:

```ts
    discountSource: 'coupon' | 'transfer' | 'promo2x1' | null;
    /** Desglose cuando hay más de un descuento (2x1 + transferencia).
     *  Las órdenes anteriores a la promo no lo tienen — es opcional a propósito.
     *  `discountAmount` sigue siendo la SUMA de todos. */
    discounts?: Array<{ source: string; amount: number; percent?: number; detail?: string }>;
```

- [ ] **Step 2: Aplicar el 2x1 antes del 10%**

En `pages/api/create-transfer-order.js`, añadir al import del tope:

```js
import { computePromo2x1, tienePromo2x1 } from '@/lib/promo2x1';
```

Reemplazar el bloque de las líneas 209-231 (desde `const productsTotal` hasta el `const total = subtotal - discountAmount;`) por:

```js
        const productsTotal = normalizedItems.reduce((sum, i) => sum + i.price * i.quantity, 0);
        const subtotal = productsTotal + shippingCost;

        // 2x1 primero: el 10% de transferencia corre sobre lo que queda.
        // El flag viene del doc de Firestore releído arriba, no del cliente.
        const { descuento: promoDiscount, unidadesGratis } = computePromo2x1(normalizedItems);
        const hayPromo = tienePromo2x1(normalizedItems);
        const subtotalConPromo = subtotal - promoDiscount;

        const discounts = [];
        if (promoDiscount > 0) {
            discounts.push({
                source: 'promo2x1',
                amount: promoDiscount,
                detail: `2x1 — ${unidadesGratis} ${unidadesGratis === 1 ? 'unidad gratis' : 'unidades gratis'}`,
            });
        }

        // Default: descuento por transferencia (−10% sobre el subtotal ya con 2x1).
        let coupon = null;
        let discountSource = 'transfer';
        let discountPercent = Math.round(TRANSFER_DISCOUNT_RATE * 100);
        let transferDiscount = Math.round(subtotalConPromo * TRANSFER_DISCOUNT_RATE);

        // Cupón válido REEMPLAZA al −10%, pero NO acumula con el 2x1.
        const couponCode = normalizeCouponCode(body.couponCode);
        if (couponCode && hayPromo) {
            return res.status(400).json({
                error: 'El cupón no es acumulable con la promo 2x1.',
            });
        }
        if (couponCode) {
            const couponSnap = await db.collection('coupons').doc(couponCode).get();
            const couponData = couponSnap.exists ? couponSnap.data() : null;
            if (isCouponValid(couponData).valid) {
                coupon = couponData.code || couponCode;
                discountSource = 'coupon';
                discountPercent = Number(couponData.percent);
                transferDiscount = computeCouponDiscount(productsTotal, discountPercent);
            }
        }

        discounts.push({
            source: discountSource,
            amount: transferDiscount,
            percent: discountPercent,
        });

        if (hayPromo) discountSource = 'promo2x1';
        const discountAmount = promoDiscount + transferDiscount;
        const total = subtotal - discountAmount;
```

- [ ] **Step 3: Guardar el desglose en la orden**

En el mismo archivo, en el objeto que va a `orderRef.set(...)`, después de `discountAmount,`:

```js
                discounts,
```

- [ ] **Step 4: Verificar**

Run: `npx next build`
Expected: `✓ Compiled successfully`

Run: `node tests/checkout-logic.test.mjs && node tests/promo2x1.test.mjs && node tests/coupons.test.mjs`
Expected: los tres PASS

- [ ] **Step 5: Commit**

```bash
git add pages/api/create-transfer-order.js lib/types.ts
git commit -m "feat(promo): 2x1 en el cobro por transferencia, acumulando con el 10%"
```

---

### Task 6: Cobro por MercadoPago — `create-preference.js`

El punto riesgoso. Lo que se le manda a MP es lo que MP cobra.

**Files:**
- Modify: `pages/api/create-preference.js:213-240`

**Interfaces:**
- Consumes: `computePromo2x1`, `tienePromo2x1`, `repartirDescuentoEnItems` de Task 1; forma de `discounts[]` de Task 5
- Produces: nada nuevo

- [ ] **Step 1: Aplicar el 2x1 y repartir los precios**

Añadir al import del tope de `pages/api/create-preference.js`:

```js
import { computePromo2x1, tienePromo2x1, repartirDescuentoEnItems } from '@/lib/promo2x1';
```

Reemplazar el bloque desde `const productsTotal = normalizedItems.reduce(...)` (línea ~213) hasta `const total = discountedProductsTotal + shippingCost;` por:

```js
        const productsTotal = normalizedItems.reduce((sum, i) => sum + (i.price * i.quantity), 0);

        // 2x1: el flag y el precio salen del doc de Firestore releído arriba,
        // nunca del body. El descuento se reparte bajando los unit_price de las
        // líneas en promo, porque MP no acepta una línea de descuento.
        const { descuento: promoDiscount, unidadesGratis } = computePromo2x1(normalizedItems);
        const hayPromo = tienePromo2x1(normalizedItems);

        const discounts = [];
        if (promoDiscount > 0) {
            discounts.push({
                source: 'promo2x1',
                amount: promoDiscount,
                detail: `2x1 — ${unidadesGratis} ${unidadesGratis === 1 ? 'unidad gratis' : 'unidades gratis'}`,
            });
        }

        // Cupón válido: baja el unit_price de cada producto en su %. NO acumula
        // con el 2x1.
        let coupon = null;
        let discountSource = hayPromo ? 'promo2x1' : null;
        let discountPercent = 0;
        const couponCode = normalizeCouponCode(body.couponCode);
        if (couponCode && hayPromo) {
            return res.status(400).json({
                error: 'El cupón no es acumulable con la promo 2x1.',
            });
        }
        if (couponCode) {
            const couponSnap = await db.collection('coupons').doc(couponCode).get();
            const couponData = couponSnap.exists ? couponSnap.data() : null;
            if (isCouponValid(couponData).valid) {
                coupon = couponData.code || couponCode;
                discountSource = 'coupon';
                discountPercent = Number(couponData.percent);
                discounts.push({ source: 'coupon', amount: 0, percent: discountPercent });
            }
        }

        const discountedItems = promoDiscount > 0
            ? repartirDescuentoEnItems(normalizedItems, promoDiscount)
            : normalizedItems.map((i) => ({
                ...i,
                unitPrice: discountPercent ? Math.round(i.price * (100 - discountPercent) / 100) : i.price
            }));

        const discountedProductsTotal = discountedItems.reduce((sum, i) => sum + i.unitPrice * i.quantity, 0);
        const discountAmount = productsTotal - discountedProductsTotal;
        if (discounts.length && discounts[discounts.length - 1].source === 'coupon') {
            discounts[discounts.length - 1].amount = discountAmount;
        }
        const subtotal = productsTotal + shippingCost;
        const total = discountedProductsTotal + shippingCost;
```

- [ ] **Step 2: Guardar el desglose en la orden**

En el objeto que va a `orderRef.set(...)`, después de `discountAmount,`:

```js
                discounts,
```

- [ ] **Step 3: Verificar el invariante contra el código real**

El invariante ya está cubierto por `tests/promo2x1.test.mjs` (Task 1) sobre `repartirDescuentoEnItems`. Acá se verifica que el endpoint lo use bien: `mpItems` se arma desde `discountedItems` (línea ~281, sin cambios), y `total = discountedProductsTotal + shippingCost`. Como `discountedProductsTotal` es exactamente `sum(unitPrice × quantity)` y `mpItems` mapea 1:1 esos mismos valores más la línea de envío, la suma cierra por construcción.

Confirmar leyendo que `mpItems` sigue siendo `discountedItems.map(...)` y que no haya quedado ninguna referencia a `normalizedItems` en el armado de los ítems de MP:

Run: `grep -n "mpItems\|discountedItems" pages/api/create-preference.js`
Expected: `mpItems` se construye solo desde `discountedItems`.

- [ ] **Step 4: Verificar**

Run: `npx next build`
Expected: `✓ Compiled successfully`

Run: `node tests/promo2x1.test.mjs && node tests/checkout-logic.test.mjs && node tests/stock.test.mjs`
Expected: los tres PASS

- [ ] **Step 5: Commit**

```bash
git add pages/api/create-preference.js
git commit -m "feat(promo): 2x1 en MercadoPago repartiendo el descuento en los unit_price"
```

---

### Task 7: El resumen del checkout y el mensaje de WhatsApp

Que el cliente vea la promo desglosada y entienda por qué su cupón fue rechazado.

**Files:**
- Modify: `components/checkout/CheckoutModal.tsx:616-640`
- Modify: `lib/checkout.js` (`buildTransferWaMessage`, línea ~112)

**Interfaces:**
- Consumes: `promoDiscount` de `computeCheckoutTotals` (Task 4); `tienePromo2x1` de Task 1
- Produces: nada

- [ ] **Step 1: Línea de la promo en el resumen**

En `components/checkout/CheckoutModal.tsx`, dentro del `<ul>` del resumen, inmediatamente **antes** del bloque `{coupon ? (`:

```tsx
                                    {totals.promoDiscount > 0 && (
                                        <li className="volt-summary-discount">
                                            <span>Promo 2x1 ({totals.promoUnidadesGratis} gratis)</span>
                                            <span>−{formatMoney(totals.promoDiscount)}</span>
                                        </li>
                                    )}
```

- [ ] **Step 2: Aviso de cupón bloqueado**

En el mismo archivo, donde está el input del cupón, deshabilitarlo cuando hay promo. Localizar el input del cupón con:

Run: `grep -n "couponCode\|setCouponCode" components/checkout/CheckoutModal.tsx`

Añadir junto al input, usando el helper ya importado:

```tsx
                                    {tienePromo2x1(items) && (
                                        <p className="volt-coupon-blocked">
                                            El cupón no es acumulable con la promo 2x1.
                                        </p>
                                    )}
```

Y añadir `disabled={tienePromo2x1(items)}` al `<input>` del código de cupón y al botón que lo aplica.

Añadir al import del tope del archivo:

```tsx
import { tienePromo2x1 } from "@/lib/promo2x1";
```

- [ ] **Step 3: Línea en el mensaje de WhatsApp**

En `lib/checkout.js`, en `buildTransferWaMessage`, el array que se devuelve tiene una línea de descuento condicional. Reemplazar esa línea única por dos entradas, insertando la promo antes:

```js
        `*Subtotal:* ${formatMoney(subtotal)}`,
        ...(serverTotals.promoDiscount > 0
            ? [`*Promo 2x1:* −${formatMoney(serverTotals.promoDiscount)}`]
            : []),
        serverTotals.discountSource === 'coupon'
            ? `*Descuento cupón ${serverTotals.coupon} (${serverTotals.discountPercent}%):* −${formatMoney(discountAmount)}`
            : `*Descuento 10% transferencia:* −${formatMoney(discountAmount - (serverTotals.promoDiscount || 0))}`,
        `*TOTAL A TRANSFERIR: ${formatMoney(finalTotal)}*`,
```

Nota: se resta `promoDiscount` de `discountAmount` en la línea del 10% porque `discountAmount` es la **suma** de todos los descuentos, y la promo ya se listó aparte.

- [ ] **Step 4: Verificar**

En `components/checkout/CheckoutModal.tsx`, confirmar que `promoDiscount` viaja del servidor al display: el `setState` de la línea ~364 debe incluir `promoDiscount: data.promoDiscount` si el endpoint lo devuelve. Si el endpoint no lo devuelve en su JSON de respuesta, añadirlo en la respuesta de ambos endpoints junto a `discountAmount`.

Run: `npx next build`
Expected: `✓ Compiled successfully`

Run: `node tests/checkout-logic.test.mjs`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add components/checkout/CheckoutModal.tsx lib/checkout.js
git commit -m "feat(promo): desglose del 2x1 en el resumen y en el mensaje de WhatsApp"
```

---

### Task 8: Verificación end-to-end y cierre

**Files:**
- Modify: `CLAUDE.md` (sección de comandos, si corresponde)

- [ ] **Step 1: Correr toda la batería**

```bash
node tests/promo2x1.test.mjs
node tests/checkout-logic.test.mjs
node tests/brain.test.mjs
node tests/coupons.test.mjs
node tests/stock.test.mjs
node tests/admin-product-form.test.mjs
node tests/cart-merge.test.mjs
npx next build
```
Expected: todos PASS, build verde.

- [ ] **Step 2: E2E**

Run: `npm run test:e2e`
Expected: PASS. Son solo lectura (no escriben en Firestore ni crean preferencia de MP).

- [ ] **Step 3: QA manual en preview**

Lista de verificación, con el preview deploy de la rama:

1. Admin → Productos → editar un buzo TC → tildar "Entra en promo 2x1" → guardar.
2. Catálogo: el sello "2x1" aparece en esa card y no tapa "Edición limitada".
3. Agregar 2 unidades al carrito → el resumen muestra "Promo 2x1 (1 gratis)" y el total correcto.
4. Agregar una 3ra → el descuento sigue siendo de una sola unidad.
5. Intentar aplicar un cupón → aparece "no es acumulable con la promo 2x1" y el input está deshabilitado.
6. Elegir transferencia → verificar que el 10% corre sobre el subtotal ya descontado.
7. Destildar el flag en el admin → recargar el catálogo → el sello desaparece.

**Advertencia:** el preview escribe en el Firestore real (CLAUDE.md). No completar pagos de prueba salvo con productos de prueba dedicados, y borrar las órdenes de QA por id desde el panel.

- [ ] **Step 4: Commit final y merge**

```bash
git add -A
git commit -m "test(promo): verificación end-to-end de la promo 2x1"
```

Merge a `main` solo después de la lista de QA. `main` es producción y despliega solo.

---

## Self-Review

**Cobertura del spec:**

| Requisito del spec | Tarea |
|---|---|
| Flag `promo2x1` por producto, tildado en admin | 2 |
| Regla: el más barato de cada par, todos los pares | 1 |
| Acumula con transferencia, bloquea cupones | 4, 5, 6, 7 |
| Módulo puro `lib/promo2x1.js` consumido por los tres | 1, 4, 5, 6 |
| Servidor autoridad, ignora lo que manda el cliente | 5, 6 |
| Reparto de `unit_price` para MP + invariante | 1 (lógica + test), 6 (uso) |
| `discounts[]` sin romper `discountAmount`/`discountSource` | 5 |
| Sello en el catálogo | 3 |
| `promo2x1` opcional en `CartItem` | 2, 3 |
| Batería de tests del spec | 1, 4 |
| Sin contador de stock / vencimiento / cupo | ninguna (fuera de alcance, correcto) |

Sin huecos.

**Consistencia de nombres:** `computePromo2x1`, `tienePromo2x1`, `repartirDescuentoEnItems` se usan idénticos en las tareas 1, 4, 5, 6 y 7. `promoDiscount` y `promoUnidadesGratis` son los nombres del retorno de `computeCheckoutTotals` en las tareas 4 y 7. `discounts[]` tiene la misma forma en 5 y 6.

**Riesgo identificado:** la Task 7 Step 4 depende de que los endpoints devuelvan `promoDiscount` en su JSON de respuesta. Está marcado explícitamente como paso condicional a verificar, no como supuesto.
